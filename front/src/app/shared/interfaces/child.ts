export interface Child {
    id: number; // Identifiant unique de l'enfant
    name: string; // Nom de l'enfant
    groupId: number; // ID du groupe auquel il appartient
    age: number;  // Ajout de la propriété 'age'
  class: string;  // Ajout de la propriété 'class'
  }
  